let fs,gulp,path;_4d5‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_4d5‍.w("gulp",[["default",["gulp"],function(v){gulp=v}]]);_4d5‍.w("path",[["default",["path"],function(v){path=v}]]);



const tasks = fs.readdirSync("./gulp/tasks/");

global.rootPath = path.dirname(__dirname);

// Ensure process ends after all Gulp tasks are finished
gulp.on("stop", function() {
  if (!global.isWatching) {
    process.nextTick(function() {
      process.exit(0);
    });
  }
});

tasks.forEach(task => {
  require("./tasks/" + task);
});

gulp.task("default", () => {
  gulp.run("templates");
});
